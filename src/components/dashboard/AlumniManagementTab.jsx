// Alumni features have been disabled for University Admins.
// This component is intentionally left blank to prevent import errors if referenced, 
// though it should be removed from the main dashboard routing.
const AlumniManagementTab = () => null;
export default AlumniManagementTab;